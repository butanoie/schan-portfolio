{
  "enabledPlugins": {},
  "permissions": {
    "ask": [
      "Bash(git commit:*)",
      "Bash(gh issue close:*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|Read",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_FILE_PATH\" | grep -qE '\\.env\\.local$'; then echo 'BLOCKED: Cannot read/edit .env.local (contains secrets)' >&2; exit 1; fi"
          }
        ]
      }
    ]
  }
}
