name: Manual Deploy to Development

on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git reference to deploy (branch, tag, or commit SHA)'
        required: false
        default: 'main'
        type: string

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    # ⚠️ REQUIRES MANUAL APPROVAL
    # This deployment is protected by environment rules.
    # A reviewer must approve before deployment proceeds.
    # Configure in: Settings → Environments → Sing Portfolio / development → Required reviewers
    environment: "Sing Portfolio / development"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref || 'main' }}

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Set SENTRY_RELEASE on Railway
        run: railway variable set SENTRY_RELEASE=$DEPLOY_SHA --project=${{ vars.RAILWAY_PROJECT_ID }} --service=${{ vars.RAILWAY_SERVICE_NAME }} --environment=development --skip-deploys
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          DEPLOY_SHA: ${{ github.sha }}

      - name: Deploy to Railway Development
        run: railway up --project=${{ vars.RAILWAY_PROJECT_ID }} --service=${{ vars.RAILWAY_SERVICE_NAME }} --environment=development
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

      - name: Deployment Summary
        env:
          DEPLOYED_REF: ${{ github.event.inputs.ref || 'main' }}
          DEPLOYED_BY: ${{ github.actor }}
        run: |
          echo "✅ Deployment to development complete"
          echo "📋 Deployed ref: $DEPLOYED_REF"
          echo "👤 Deployed by: $DEPLOYED_BY"
          echo "⏰ Timestamp: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
